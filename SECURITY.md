# 安全说明 Security

## 本项目的数据安全设计

- 纯前端应用，**无后端、无数据库、无 Cookie**。
- API Key 只保存在你自己的浏览器 `localStorage` 中，请求由浏览器直连服务商，项目代码不会上传你的 Key。
- 本地服务器 `serve.js` 只监听 `127.0.0.1` 回环地址，不会把文件服务暴露到局域网。

## 报告漏洞

如果发现安全问题（例如 XSS、路径穿越、敏感信息泄露），请不要公开讨论，直接通过 [GitHub Issues](https://github.com/zoushuling/ai-chinese-chess/issues) 私密反馈，或发邮件到仓库主页的联系邮箱，说明：

- 影响版本 / 运行方式
- 复现步骤
- 影响与建议修复方案（可选）

我们会尽快处理并在修复后致谢（如你愿意署名）。
